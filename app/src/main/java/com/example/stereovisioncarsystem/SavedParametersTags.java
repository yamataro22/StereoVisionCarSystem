package com.example.stereovisioncarsystem;

public enum SavedParametersTags {
    Thresh, Gauss, NbOfStereoCalibrationFrames, QMatrix, IsThreshInverted, R1, R2, T1, T2, R, T, E, F,clientCameraMatrix,clientDistCoeffs,
    minRatio,maxRatio,minArea,maxArea, client, server
}
